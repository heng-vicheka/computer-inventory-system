<div class="row justify-content-center">
	<div class="col-lg-6">
		<div class="d-flex align-items-center gap-2 mb-4">
			<a href="/items" class="btn btn-sm btn-outline-secondary">
				<i class="bi bi-arrow-left"></i>
			</a>
			<div>
				<h2 class="fw-bold mb-0">Check In Item</h2>
				<p class="text-muted mb-0">Return an item and mark it available</p>
			</div>
		</div>

		<div class="card border-0 shadow-sm">
			<div class="card-body p-4">
				<form action="/transactions/checkin" method="POST" enctype="multipart/form-data" novalidate>
					<div class="mb-3">
						<label for="itemId" class="form-label fw-semibold">Item <span class="text-danger">*</span></label>
						<select id="itemId" name="itemId" class="form-select" required>
							<option value="" disabled selected>Select in-use item</option>
							{{#each inUseItems}}
							<option
								value="{{this._id}}"
								{{#if (eq ../selectedItemId this._id)}}selected{{/if}}
							>{{this.brand}} {{this.model}} — {{this.serialNumber}} (assigned to {{this.assignedTo.username}})</option>
							{{/each}}
						</select>
					</div>

					<div class="mb-4">
						<label for="document" class="form-label fw-semibold">
							Inspection Document <span class="text-danger">*</span>
							<span class="text-muted fw-normal small">(PDF, image)</span>
						</label>
						<input
							type="file"
							id="document"
							name="document"
							class="form-control"
							accept=".pdf,.jpg,.jpeg,.png"
							required
						/>
					</div>

					<div class="d-flex justify-content-end gap-2">
						<a href="/items" class="btn btn-outline-secondary">Cancel</a>
						<button type="submit" class="btn btn-success">
							<i class="bi bi-box-arrow-in-down me-1"></i>Confirm Check In
						</button>
					</div>
				</form>
			</div>
		</div>
	</div>
</div>
